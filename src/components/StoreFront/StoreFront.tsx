import styles from './StoreFront.module.scss';

const StoreFront = () => {
  return (
    <div className={styles['store-front']}>
      <h2>Products</h2>
    </div>
  );
};

export default StoreFront;
